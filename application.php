<?php

/* ////////////////////////////////////////////////////////////////////////////// */
/* ////////////////////////////////////////////////////////////// APPLICATION /// */
/* ////////////////////////////////////////////////////////////////////////////// */

/* ------------------------------------------------------------------- CONFIG --- */
include __DIR__ . '/config.php';

/* -------------------------------------------------------------- ENVIRONMENT --- */
$_ENVIRONMENT = environment();

/* ---------------------------------------------------------------------- ENV --- */
$_ENV = $_CONFIG[$_ENVIRONMENT];

/* -------------------------------------------------------------------- ERROR --- */
error_reporting($_ENV['error']);

/* ----------------------------------------------------------------- TIMEZONE --- */
date_default_timezone_set('Europe/London');

/* --------------------------------------------------------------------- ROOT --- */
define('ROOT_URL', implode('', ['http', encrypted() ? 's' : '', '://', $_SERVER['HTTP_HOST'], $_ENV['root'],]));
define('ROOT_DIR', dirname(__FILE__));

/* ---------------------------------------------------------------------- DIR --- */
define('DIR_TMP', '/tmp');
define('DIR_LIB', '/lib');
